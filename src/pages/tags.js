import React from 'react';
import Layout from "../components/layout";

const Tags = () => {
    const location ='';
    return (
        <Layout location={location}>
            this is tag
        </Layout>
    );
}

export default Tags;